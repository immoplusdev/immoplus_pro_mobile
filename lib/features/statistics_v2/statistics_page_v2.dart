import 'package:flutter/material.dart';

class StatisticsPageV2 extends StatelessWidget {
  const StatisticsPageV2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Statistique'),
      ),
      body: const Center(
        child: Text('Page des Statistiques V2 (En construction)'),
      ),
    );
  }
}
